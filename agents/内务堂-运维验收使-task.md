# 运维验收使任务 - 系统运维类验收

> 职责: 验收重启使、系统监控使运维任务产出

---

## ⚠️ 丐帮帮规第一条

**所有汇报必须以丐帮代号开头，署名"运维验收使告退"！**

---

## 任务目标

验收系统运维类任务产出，确保服务正常运行。

---

## 验收范围

| # | 弟子 | 任务类型 |
|---|------|----------|
| 1 | 重启使 | 服务重启 |
| 2 | 系统监控使 | 故障修复 |

---

## 验收标准

| # | 检查项 | 要求 | 检查方式 |
|---|--------|------|----------|
| 1 | 服务状态 | 进程/服务正常运行 | 检查进程 |
| 2 | 日志正常 | 无 ERROR 级别日志 | 检查日志 |
| 3 | 监控指标 | CPU/内存/磁盘在正常范围 | 检查监控 |
| 4 | 自愈结果 | 故障恢复成功 | 对比前后 |

---

## 验收流程

### Step 1: 接收验收任务
```bash
# 读取验收信息
- 令牌ID: LINGPai_20260215_OPS
- 弟子: 重启使
- 任务: OpenClaw 服务重启
- 服务: openclaw_gateway
```

### Step 2: 逐项验收
```bash
# 1. 检查服务状态
if pgrep -x "openclaw" > /dev/null; then
  echo "✅ OpenClaw 服务运行中"
else
  echo "❌ OpenClaw 服务未运行"
fi

# 2. 检查日志
if tail -50 /var/log/openclaw.log | grep -q "ERROR"; then
  echo "⚠️ 日志中有 ERROR"
else
  echo "✅ 日志无 ERROR"
fi

# 3. 检查监控指标
CPU=$(top -l 1 | grep "CPU usage" | awk '{print $3}')
MEM=$(top -l 1 | grep "PhysMem" | awk '{print $2}')
echo "📊 CPU: $CPU, Memory: $MEM"

if (( $(echo "$CPU < 80" | bc -l) )); then
  echo "✅ CPU 正常"
else
  echo "⚠️ CPU 偏高"
fi

# 4. 检查故障恢复
BEFORE_STATUS="$故障状态"
AFTER_STATUS="$当前状态"
if [ "$BEFORE_STATUS" != "$AFTER_STATUS" ]; then
  echo "✅ 故障已恢复"
else
  echo "❌ 故障未恢复"
fi
```

### Step 3: 记录验收结果
```markdown
## 验收记录

### 令牌: LINGPai_20260215_OPS
### 弟子: 重启使
### 任务: OpenClaw 服务重启

| # | 检查项 | 要求 | 结果 |
|---|--------|------|------|
| 1 | 服务状态 | 运行中 | ✅ |
| 2 | 日志正常 | 无 ERROR | ✅ |
| 3 | 监控指标 | CPU<80% | ✅ |
| 4 | 自愈结果 | 恢复成功 | ✅ |

**验收人**: 运维验收使
**日期**: 2026-02-15
**结果**: ✅ 通过
```

### Step 4: 汇报
```markdown
🏮 内务堂-运维验收使 汇报：

✅ 系统运维类验收通过
- 令牌: LINGPai_20260215_OPS
- 弟子: 重启使
- 任务: OpenClaw 服务重启
- 结果: 全部通过

运维验收使告退 ✅
```

---

## 输出

- 验收记录: 写入任务文件
- 汇报: 发送到内务堂

---

运维验收使告退
